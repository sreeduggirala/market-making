[package]
name = "risk"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
adapters = { path = "../adapters" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
chrono = "0.4"
rust_decimal = "1"
thiserror = "1"
log = "0.4"
anyhow = "1"
